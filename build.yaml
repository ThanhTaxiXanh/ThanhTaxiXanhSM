# build.yaml - Cấu hình build_runner cho Drift và Riverpod code generation

targets:
  $default:
    builders:
      # === DRIFT: generate .g.dart từ schema ===
      drift_dev:
        options:
          # Tạo migration SQL khi schema thay đổi
          store_date_time_values_as_text: true
